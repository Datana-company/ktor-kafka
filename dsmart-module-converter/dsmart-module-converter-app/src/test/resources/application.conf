ktor {
  deployment {
    port = 8080
    port = ${?PORT}
  }
  kafka {
    bootstrap.servers = "172.29.40.58:9092"
    client.id = "kafkaClientId"
    consumer {
      group.id = "kafkaGroupId"
    }
  }
}
