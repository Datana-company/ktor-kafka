import { RecommendationModel } from './recommendation-model';

describe('RecommendationModel', () => {
  it('should create an instance', () => {
    expect(new RecommendationModel()).toBeTruthy();
  });
});
