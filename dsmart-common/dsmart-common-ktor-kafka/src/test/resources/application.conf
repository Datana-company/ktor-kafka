ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        id = "ru.datana.smart.common.ktor.kafka.TestKafkaApplication"
        modules = [ru.datana.smart.common.ktor.kafka.TestKafkaApplicationKt.module]
    }
    kafka {
        client.id = "kostya_kotlin"
        bootstrap.servers = ["worker1.datana.ru:9092"]
        producer {
            ;           client.id = "temperature-ui-consumer"
            ;           key.serializer = "org.apache.kafka.common.serialization.LongSerializer"
            ;           value.serializer = "me.viartemev.ktor.me.viartemev.ktor.kafka.JacksonSerializer"
        }
        consumer {
            topic = "ui-temperature"
            group.id = "temperature-ui"
            ;           key.deserializer = "org.apache.kafka.common.serialization.LongDeserializer"
            ;           value.deserializer = "me.viartemev.ktor.me.viartemev.ktor.kafka.JacksonDeserializer"
        }
    }
}
